{
  "name": "posture-review-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop app that captures webcam posture snapshots, scores them over time, and generates posture reviews.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check electron/main.js && node --check electron/preload.js && node --check src/app.js && node --check src/postureScoring.js && node --check src/localReview.js",
    "test": "node test/postureScoring.test.js && node test/localReview.test.js"
  },
  "devDependencies": {
    "electron": "^42.4.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
